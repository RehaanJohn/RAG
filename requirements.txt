fastapi>=0.111.0
uvicorn[standard]>=0.29.0
asyncpg>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Embeddings — runs fully locally, no external calls
sentence-transformers>=3.0.0
# CPU-only torch build (reduce Docker image size).
# For GPU, remove --extra-index-url and install torch with CUDA support.
torch>=2.3.0

# Async HTTP client (Ollama calls)
httpx>=0.27.0

# Background jobs
arq>=0.26.0
redis>=5.0.0

# File parsing
pdfplumber>=0.10.0

# Token counting
tiktoken>=0.7.0

# Multipart form uploads
python-multipart>=0.0.9
